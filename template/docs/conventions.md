# Project conventions

Everything in this file is given to every role, in every session. Keep it short: it is
paid for on every single run.

## Stack

<!-- Replace with the real stack once it exists. -->

- Language:
- Package manager:
- Test command:
- Build command:

## Rules

- Match the surrounding code. New patterns need a reason stated in the design.
- No new dependency unless the design document names it.
- Never commit or push unless the task explicitly asks for it.
- Secrets stay in `.env`; never hardcode credentials.

## Layout

<!-- Describe the directories that matter, so agents stop guessing. -->
